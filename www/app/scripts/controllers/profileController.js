/* global bowlineApp, moment, Spinner, Flatdoc, io, $, lil */

bowlineApp.controller('profileController', 
	['$scope', '$sce', '$location', '$http', 'loginModule', 'releaseModule', '$timeout', 'ENV', 
	function($scope,$sce,$location,$http,login,release,$timeout,ENV) {

		console.log("!trace profileController init");

	}
]);
